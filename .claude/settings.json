{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(jq -r \".tool_input.command\" <<< \"$(cat)\"); for p in \"rm -rf /\" \"rm -rf ~\" \"DROP TABLE\" \"TRUNCATE\" \"push.*--force\"; do if echo \"$CMD\" | grep -qiE \"$p\"; then echo \"BLOCKED: dangerous command pattern detected\" >&2; exit 2; fi; done; exit 0'"
          }
        ]
      }
    ]
  }
}
