// demo-video PACK — narration.mjs  (AUTHOR FIRST; calibrated language only —
// consistent with / re-derived / refuted / untested / flagged. Never discovered/proven/definitive.)
// Falsification-first arc: fast YES (credibility) -> UNTESTED (the centerpiece, halts at the gate) ->
// confident NO (disease-specific) -> the cull/funnel -> Claude Science depth.

export const TURNS = {
  walk: [
    { id: "s1_yes", text:
      "Start with a claim: that NAB2 shapes the Th1/Th2 program tied to atopic eczema. " +
      "The referee checks it hop by hop — the knockdown is real, the downstream effect is real, " +
      "it shifts the program, and it lands in atopic-eczema modules. Every step, a receipt." },
    { id: "s2_untested", text:
      "Now the part that makes this a referee. We ask the same of SATB1 in asthma — but it stops at the " +
      "very first gate. SATB1's knockdown never took; the guide barely moved its expression. So the " +
      "downstream is uninterpretable, and the honest verdict is untested — not a negative. " +
      "Catching the failed experiment is the whole point." },
    { id: "s3_no", text:
      "And it is disease-specific. The same NAB2, the same real knockdown and effect — but ask about " +
      "multiple sclerosis, and the chain holds right up to the disease hop, where it refuses. A confident no." },
    { id: "s4_funnel", text:
      "This is where the questions came from. The dataset arrived with none, so the engine generated " +
      "twenty-two thousand gene-and-disease questions — and the referee refused all but the thirty it " +
      "could back with a receipt at every hop." },
    { id: "s5_depth", text:
      "Finally, an independent Claude Science agent — given only the data and the question — reasoned " +
      "through the same chain, refuted the sharpest confounder that this is just a STAT6 shadow, and " +
      "arrived at the same verdict on its own." },
  ],
};

export const CARDS = {
  title: { text:
    "PyZoBot Arbiter — a referee for T-cell hypotheses. Give it a gene, a program, and a disease, " +
    "and it answers with a receipt for every step. And it is willing to say a confident no." },
  endcap: { text:
    "Receipt-backed. Willing to refute. Independently replicated. That is PyZoBot Arbiter." },
};
