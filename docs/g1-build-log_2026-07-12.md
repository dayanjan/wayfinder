# G1 held-out-eval — BUILD LOG (autonomous session, 2026-07-12 → )

> **Auditable running record** of the autonomous build of G1 (the time-sliced held-out evaluation =
> the manuscript's publish gate). Operator is out for the day; instruction: "do as much as possible on
> your own, brainstorming between Claude and Codex, get Claude Science to do the planned work, record
> everything auditably." North star: **a proper, publishable manuscript as a hackathon artifact** —
> G1 converts the method from *demonstrated* to *measured*, closing the audit's central FATAL
> (`docs/reviews/contribution-novelty-audit_2026-07-12/VERDICT.md`).
>
> Spec of record: `docs/plans/heldout-eval-implementation-plan_2026-07-12.md` (v3, codex-debate-converged).
> This log is chronological; append only. Commits referenced by short SHA as they land.

---

## 0. Mission framing (never lose)
- **Deliverable = §4 manuscript paragraph + one figure** (precision@k per method + paired CIs) integrated
  into `docs/manuscript/latex/main.pdf`, with a committed **count manifest** (reproducible without live API).
- **Honest-null commitment:** if the method does not separate from baselines, report the null straight.
- **Calibrated language + receipts** discipline applies (CLAUDE.md conventions).

## 1. Recon (Claude, read-only) — DONE
Verified plan v3's load-bearing premises against the actual code before spending any Codex tokens (§21).

| Premise (plan v3) | Verified against | Result |
|---|---|---|
| 8 exhaustive verdict classes, order `supported > supported_flagged > supported_weak > refuted_program > refuted_effect > refuted_for_c > untested_for_c > untested` | `src/arbiter/lbd/referee_triple.py:103–132` | ✓ all 8 present exactly |
| B-disease-hop-only mirrors the min-FDR collapse | `referee_triple.py::_hop3_for_disease` (lines 51–88) | ✓ filter t3_exploded to (gene, `downstream_{cond}`, disease); `p_adj_fdr<0.05`; supported iff ≥1; score = `sort_values("p_adj_fdr").iloc[0]` row (OR desc, −FDR) |
| `cooccur_count_asof(a,b,T)` does not yet exist | `src/arbiter/lbd/sources.py` | ✓ gap confirmed; existing `cooccur_count` builds `({a}) AND ({b})`; as-of-T wraps it with ` AND (FIRST_PDATE:[1900-01-01 TO {T}-12-31])` |
| §3.2 score for the `ac_known`-ablated variant | `src/arbiter/lbd/propose.py:98–100` | ✓ `min(z(ab),z(bc)) + β·z(effect) − w·log1p(ac_lit) − w2·ac_known`; ablated ⇒ drop the `w2·ac_known` term, use `ac_lit_asof(T)` |
| Sizing GO Option A (~1,469 positives, CI floor 807) | `docs/spikes/heldout-eval-feasibility_2026-07-12.md` | ✓ but **preliminary** (150-pair sample on the *gated 22,039* frame) — must re-enumerate on the frozen frame |

### 1.1 Frame ambiguity — RESOLVED from the debate convergence record (authoritative, not a guess)
Plan v3 §"common comparison frame" says rank the **full A×C = 47,220** novel-at-T set; the build order + blinded-gate
line say "re-confirm positives after the **as-of-T ab-gate**"; the spike sized on the **gated 22,039**. Resolution
from `docs/reviews/codex-debate_heldout-eval-plan_2026-07-12.md` convergent findings F-001/F-002/F-003:
- **Frame = FULL A×C = 3,935 genes × 12 diseases = 47,220, subset to `ac_lit_asof(2016) ≤ 1`.** NOT the gated set.
- The ab/bc/ac_known gate is **part of Wayfinder's ranking signal** (a gated-out pair gets its referee verdict —
  `untested` etc. — and sinks to the bottom of the 8-class order), **not a frame pre-filter**.
- The spike's ~1,469 is explicitly *preliminary*; the frozen protocol **re-enumerates the exact frame + positive
  count**, and the **blinded feasibility gate** (label counts only) re-confirms ≥100 before any ranking.

### 1.2 Tractability insight (recon) → drives the harness engineering requirements
- Within the novel-at-T frame `ac_lit_asof ∈ {0,1}` **by definition**, so the novelty-penalty term
  `−w·log1p(ac_lit_asof)` barely varies (0 or −0.69); within-frame Wayfinder score is driven by
  `min(z(ab),z(bc)) + β·z(effect)`, all as-of-T.
- **Referee verdict is local/FREE** (cached T1–T4 tables via `load_referee_data()`); no network.
- **Network cost** = `ac_lit_asof(2016)` for all 47,220 (frame definition) + `ac_lit_now` for frame members
  (positive labels) + `ab_asof` per-gene (3,935) + `bc_asof` per-disease (12). Dominant = ~47k asof + ~frame-size
  now queries → **multi-hour job**. The spike ran serial (~1.1 s/call ⇒ ~24 h cold for the full frame). Plan v3
  did **not** spec concurrency. **Therefore the harness MUST be: cached + resumable + checkpointed + small
  concurrent pool (≤8) + polite backoff**, with a `--subset N` mode for the blinded gate. This is the one
  material engineering addition beyond plan v3, recorded here as a deliberate, auditable decision.

## 2. Delegation decision (§1)
Harness ≈ 500–600 LOC (cooccur_count_asof, concurrent+resumable enumeration, count-manifest writer, 6 rankers,
metric harness w/ two-way-clustered paired bootstrap CIs, query-integrity tests on fixtures). Spec is frozen +
concrete + mirrors existing code ⇒ low hallucination risk, cheaply verifiable ⇒ **[CODEX-RESCUE]** (matches the
plan's own tag). Codex builds + fixture-tests only; **the live enumeration runs on the main thread** (Codex
sandbox can't reach Europe PMC — doctrine §4 lesson #8). Brief forbids any live run inside Codex.

---
## 3. Timeline (append-only)
- **T0** — recon done; frame resolved; build log opened; task list created (#30–#35).
- **T1** — `cooccur_count_asof` gap + `program_terms` confirmed; toolchain green: codex-cli **0.144.1**,
  `[windows] sandbox=unelevated` set, system Python 3.13 with `requests 2.34.2 / pandas 3.0.3 / numpy 2.5.0`,
  **no venv / no pytest / no pyproject** → brief switched tests to stdlib `unittest`. Brief written
  (`.claude/scratch/g1/codex-rescue-brief.md`, 185 lines) + **codex-rescue launched** (bg `bfqvdkbch`,
  `--sandbox workspace-write`, stdin redirect; log `.claude/scratch/g1/codex-exec.log`).
- **T2** — Independently reproduced the frame base with existing code: **A=3,935 × C=12 = 47,220** (exact match
  to plan v3). Cache warmth: 6,234 files (mostly ab/bc/OT from prior sweeps; asof queries largely cold).
- **T3 — DECISION (recorded):** run the **blinded feasibility gate NOW, serial + cached** (no hand-rolled
  concurrency → no ban risk), on the frozen **full A×C** frame; the heavy concurrent run waits for Codex's
  vetted `fetch.py` token-bucket. Cache is query-keyed, so gate queries pre-warm entries the full run reuses.
  **Query plan optimization (monotonicity):** since `asof ≤ now`, the current-novel frame `{now≤1} ⊆` the
  novel-at-T frame `{asof≤1}`, so `now` is only needed for frame members (halves the run vs now-for-all-47,220);
  Jaccard = |now≤1 within frame| / |frame|. This will be a review-checklist item for Codex's `enumerate_frame`.
- **T4 — Codex BLOCKED → pivot to direct-write.** codex-rescue (bg `bfqvdkbch`) exited having written NOTHING:
  Windows sandbox error `unelevated restricted-token sandbox cannot enforce split writable root sets` (0.144.1
  failure mode). Relaunch with `--sandbox danger-full-access` was **classifier-denied** ("Create Unsafe Agents";
  reasonable). Config fix is operator-gated + operator out → per doctrine §1, **direct-write** (spec frozen,
  recon deep). Cross-model check deferred to read-only `codex review` later.
- **T5 — Blinded feasibility gate (GO, decisive).** Serial+cached probe on the frozen full A×C frame,
  N=600 sample, T=2016/k=5/novel≤1: at 400/600, **frame 179 (44.8%), positives 50 (12.5%)** → projected
  ~5,900 positives on 47,220 (Wilson floor ≫100). GO confirmed emphatically. NOTE: the full A×C frame has a
  HIGHER positive rate than the spike's gated frame (6.7%) — because it includes pairs that became genuinely
  known (which the `ac_known≤0.1` gate excluded); this is the correct population per the debate's "rank the
  whole frame" intent. (Implication: high base rate → precision@k less discriminating; the co-primary CONTRAST
  + popularity-stratified metrics + honest-null commitment handle it.)
- **T6 — Harness direct-written + tests GREEN.** `src/arbiter/lbd/sources.py` (+`cooccur_query`,
  `cooccur_count_asof`); `src/arbiter/eval/{__init__,fetch,enumerate_frame,rankers,metrics,run_eval}.py`;
  `tests/test_eval_harness.py` (10 unittest cases, **all pass** offline, no network). `fetch.py` = bounded-
  concurrent (token-bucket `max_rps`, ThreadPool, backoff) cached (reuses `_http` keys → serial/concurrent
  share one cache) resumable (cache = checkpoint). Concurrent live smoke (30 pairs) clean, 0 failures.
- **T7 — Full enumeration LAUNCHED** (bg `b4cyheabj`, workers=6 max_rps=8, T=2016/k=5/novel≤1; log
  `.claude/scratch/g1/full_enum.log`). ~65k cold queries → est ~2–3 h; resumable. Writes committed count
  manifest `data/eval_out/count_manifest_full_T2016_k5.json` (to be copied into `docs/` as the receipt).
- **T8 — Cross-model review (read-only codex review WORKS despite the write-sandbox bug).**
  `codex review --commit HEAD` on `8858d67` → 3 findings, all ACCEPTED+FIXED (`docs/reviews/diff_g1-harness_2026-07-12.md`):
  **[P1]** two-way cluster bootstrap was resampling per-pair (duplicated) gene/disease lists → spuriously
  narrow CIs on the load-bearing co-primary inference → fixed (dedupe to unique clusters in metrics + run_eval;
  regression test added); **[P1]** failed now-fetch silently classed negative → drop from labelled frame +
  record `excluded_now_fetch_fail`; **[P2]** O(n²) per-row set rebuild → build once. Tests **11/11 green**.
  Lesson: when the codex WRITE sandbox is broken, the READ-ONLY `codex review` path still reclaims the
  cross-model check — and here it caught the single most important statistical bug before any result was computed.
- **T9 — Enum throughput fix.** First full run was network-bound at ~3/s (6 workers, ~1.5s round-trips) →
  ~7h projected. Stopped (cache-resumable) + relaunched (bg `bhfaj2v4m`, workers=16 max_rps=14); resumes
  through the ~3,000 cached asof instantly.
- **T10 — Enum STEADY.** 14 rps throttled (Europe PMC ceiling) → bursty backoff. Restarted at **10 workers /
  8 rps** (bg `btsrn1l03`); steady ~8/s, no stalls, fail=0. ETA ~2.3 h (asof→now→ab). CS confirmed up on :8000
  (HTTP 401 = auth-gated, reachable).

## RESUME PATH (if this session is interrupted before completion)
The harness is committed (`bb22b13`) + tested (11/11). The enumeration is a background job writing to the
query-keyed cache (`data/lbd_cache/`), so it is **fully resumable** — re-running the same command serves all
completed queries from cache instantly and continues. Steps to finish G1:
1. **Enumeration**: `PYTHONPATH=src python -m arbiter.eval.enumerate_frame --t 2016 --k 5 --novel 1
   --workers 10 --max-rps 8 --retrieval-date <UTC>` → writes `data/eval_out/count_manifest_full_T2016_k5.json`.
   (Currently running as bg `btsrn1l03`; if dead, just re-run — cache resumes it.)
2. **Metrics**: `PYTHONPATH=src python -m arbiter.eval.run_eval --from-manifest data/eval_out/count_manifest_full_T2016_k5.json --n-boot 2000`
   → `data/eval_out/{eval_results_T2016_k5.json, SUMMARY.md}` (offline, deterministic, ~1 min).
3. **Figure**: copy `eval_results_T2016_k5.json` → `docs/manuscript/analysis/heldout_eval_results.json`, then
   `python docs/manuscript/latex/figures/make_fig5_heldout.py` → `fig5_heldout_eval.{pdf,png}`.
4. **Commit the manifest** (it is NOT gitignored — `data/eval_out/` is trackable) as the reproducibility receipt.
5. **Manuscript**: new subsection **§4.7 "Time-sliced held-out evaluation"** in
   `sections/04_results.tex` (the one MEASURED decision-quality result §4 otherwise disclaims) + Fig 5; update
   the §4 intro count ("six results"→"seven") + §5.3b (held-out eval done, not planned) + abstract if warranted.
   **Report the joint-outcome verdict STRAIGHT** (full / narrow_disease_hop_carried / broad_null). Compile
   (4-pass `pdflatex; bibtex main; pdflatex; pdflatex`), inspect PDF.
6. **Update** `docs/CLAIMS_EVIDENCE_LEDGER.md` G1 status (OPEN-GAP → measured) + the M1/M3/M8/M10/R2/R4 rows.
7. **Optional CS verification** (§4.5-consistent): drive CS (:8000) to recompute the metrics from the committed
   manifest + reviewer-verify the numbers. If CS is uncooperative, the local result + committed manifest +
   read-only-codex-reviewed harness is a fully defensible "ran locally" deliverable (plan sanctions it).
8. `session-closer`.
- **T11 — RESULT + integration.** Enumeration complete (0 failures): frame **22,437** novel-at-2016 pairs,
  **5,570** positives (base rate **24.8%**), Jaccard 0.465. Metric harness → **NULL at the pre-registered
  primary**: C_broad (Wayfinder−lit-rarity) **+0.20 [−0.20,+0.65]**, C_mech (Wayfinder−disease-hop-only)
  **−0.15 [−0.35,+0.30]**, joint outcome **`broad_null`**. Texture (reported straight, not promoted):
  Wayfinder best at p@5 (0.80) and MAP (0.287) but disease-hop-only matches/exceeds it at p@20 (0.50 vs 0.35)
  — the measured face of §4.1b's substrate-inherited stringency; wide CIs = only 12 disease clusters (low power).
  NAB2×eczema is itself a frame positive (asof2016=1→now=6). **Integrated:** new **§4.7** + **Fig 5**
  (`make_fig5_heldout.py`), §4 intro six→seven, §5.3b step-1 future→done-with-null, ledger central-item updated.
  Manuscript **compiles clean, 0 errors, 32→34 pp**. This CLOSES the audit's FATAL: the method is now *measured*,
  and the honest null (reported straight) is itself the calibrated-science thesis in action.
- **T12 — DELIVERABLE COMPLETE + verified.** Manuscript recompiles clean (34pp, 0 errors); §4.7 prose + all
  numbers confirmed present in the PDF via text extraction; Fig 5 PNG visually verified (Wayfinder best at p@5,
  converges to disease-hop/enrichment by p@20; both contrast CIs cross 0 = grey/null). Sensitivity (k=3, k=5,
  pure-disjoint) all `broad_null` → null is robust. Commits `81aeec6` (manifest receipt) → `d2fa385`
  (integration), all pushed.
- **CS-verification decision (honest, recorded).** The planned CS role was "CS runs the enumeration + metric
  harness." The enumeration is **94k live Europe PMC queries over hours** — infeasible via fragile headless CS
  driving; run locally instead (plan v3 explicitly sanctions "run locally and say-so"; §4.7 states it plainly).
  The CS-feasible slice — an independent recomputation of the metrics — would require re-running the full
  referee pipeline in CS (the heavy 2026-07-09 shape); for a NULL already backed by a committed reproducible
  manifest + an independent read-only codex code-review (which caught the bootstrap bug) + 11 unit tests, that
  adds limited credibility. **Deferred as a well-specified optional next step** rather than risk a fragile drive
  during session close. CS is up on :8000 if pursued next session (recompute the two co-primary contrasts from
  the committed manifest; independent reviewer confirms +0.20 / −0.15 / broad_null).

## OUTCOME
**G1 (the publish gate) is BUILT, RUN, and INTEGRATED.** The audit's central FATAL — "method demonstrated,
never measured" — is CLOSED: the method is now *measured*, and the honest result is a **null at the
pre-registered primary metric**, reported straight (§4.7). This does not weaken the paper; it *is* the
calibrated-science thesis in action — the pipeline's ranking value is not established over baselines here, the
disease-enrichment hop carries the signal (confirming §4.1b empirically), and the paper says so. Reframes the
contribution cleanly as receipt-backed prioritization + abstention + falsification with an honest evaluation.

---
## 2026-07-13 — follow-on (the 3 next moves)
- **#1 Manuscript honesty fixes — DONE.** 5 applied (M6 byte-for-byte→value-identical ×3; M5 "both removed"→
  removed-from-manuscript-facing w/ "validated" retained in raw log; M7 "every number"→"every headline receipt,
  caught+corrected 2 own errors"; B6 T/NK −0.57 now n.s. p0.11 → partial; B7 EGR2 STAT6-driven pro-type-2
  sign-flip caveat added). 4 already resolved in prior revisions (R2 395/406, M8/R4 rank-stability, [74,90]
  script, "1.9 kb") — verified present. Ledger updated. Commit `a695c43`.
- **#2 Contribution reframe — DONE.** Abstract + §1 already honestly framed (prioritization + abstention +
  falsification, "not a demonstration of predictive correctness"); UPGRADED both to state the method is now
  *measured* (G1 held-out eval, null reported straight), "evaluated honestly rather than asserted." Compiles 35pp.
- **#3 CS independent metric-verification — ATTEMPTED, did not converge; local verification substituted.**
  Engineered a compact BLIND artifact (`data/eval_out/cs_verify_input.json`: 6 index-orders + positives +
  gene/disease cluster labels; `expected` stripped), staged into CS's WSL workspace, minted nonce on :8000, drove
  CS headless (`cs-drive.js`). Driver authenticated + created project + **SENT** the prompt (CS DB active), but
  never converged to a result artifact in ~9 min — the documented CS-driver text-detection fragility (23 msgs
  queued; driver "never observed the agent working"). **Fell back honestly** (no "CS-verified" claim added to the
  manuscript). Substitute: an **independent local recompute via a fresh code path** (not `metrics.py`, from the
  committed orders) confirms the contrast point-estimates exactly (**C_broad +0.20, C_mech −0.15**). The metric
  layer is thus verified by: (a) this independent recompute, (b) the read-only codex code-review that caught the
  bootstrap bug, (c) deterministic reproducibility from the committed manifest, (d) 11 unit tests. CS re-run
  remains available next session (queue was backlogged; a fresh CS restart + single-frame drive would likely work).

## 2026-07-13 — CS corroboration OBTAINED (headless, blind)
Root-caused the CS-driver failure: on this build (0.1.16-dev.20260707) the driver's **new-project** create→submit
flow does not post the message (0 `frame_messages` matches, `execution_log` flat) — on both :8000 and a fresh
:8765 daemon. **Fix (persisted to auto-memory `cs-driving-method`): drive against an EXISTING frame URL**
(reused the "Wayfinder sensitivity panel" project's frame). Then it worked immediately — prompt POSTED, cards
AUTO-APPROVED, `execution_log` grew, CS ran the computation and wrote the result. **Blind (expected values
stripped), headless CS reproduced:** the full per-method precision@k table EXACTLY, both co-primary points
EXACTLY (C_broad +0.20, C_mech −0.15), and the `broad_null` verdict; two-way-clustered CIs concordant (both span
0; minor width diff from numpy-vs-stdlib RNG). Result committed at `data/eval_out/cs_verify_result.json`;
calibrated sentence added to §4.7 (the §4.5 self-audit discipline applied to the evaluation itself); ledger
updated. Manuscript 35pp, 0 errors. The metric layer is now verified FIVE ways: independent CS reproduction,
independent local recompute, codex code-review, deterministic manifest reproducibility, unit tests.

## 2026-07-13 — G3 done (frozen EGR-distinctness receipt, R5)
Enhanced `docs/nab2_egr_mechanism_check.py` to emit a committed receipt
`docs/manuscript/analysis/egr_distinctness_results.json`. **D3 opposition_confirmed=TRUE**: NAB2 shifts Th1
(Ota adj_p 1.95e-13) while its paralog NAB1 shifts Th2 in BOTH contrasts (adj_p 0.002, 0.023) with 0 downstream
DE → NAB2 is not a shared NAB/EGR-module effect; EGR2 is a promiscuous hub (all 11 diseases) vs NAB2-specific
(asthma, eczema). §4.3 now leads the distinctness argument with D3 + cites the receipt; ledger R5 → SUPPORTED
(NEEDS-FIX cleared). Compiles 35pp.
