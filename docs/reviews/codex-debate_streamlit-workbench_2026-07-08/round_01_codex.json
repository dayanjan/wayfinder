{
 "round_number": 1,
 "position_summary": "Claude's latency-tier split is directionally right and the async/precomputed Claude Science lane should be preserved. The hostile read is that the plan underestimates integration risk, overbuilds beyond the 3-minute demo arc, and has a few contract mismatches that could turn the flagship screen into a Potemkin-looking UI if not fixed before implementation.",
 "findings": [
  {
   "id": "F-001",
   "title": "Streamlit cannot import `arbiter` from repo root as planned",
   "severity": 5,
   "likelihood": 4,
   "blast_radius": 4,
   "priority": "P0",
   "evidence": "`docs/plans/streamlit_workbench_plan_2026-07-08.md:56` says `from arbiter.lbd.referee_triple import ...`; `requirements.txt` has only pandas/requests/h5py/s3fs and no installable package metadata. A clean `python -c \"import arbiter\"` from repo root fails with `ModuleNotFoundError`. The source package lives under `src/`, so Streamlit launched from repo root does not automatically see it.",
   "concern_path": "Builder adds `app/streamlit_app.py`, runs `streamlit run app/streamlit_app.py`, and the app dies before reaching the dynamic referee import. The plan correctly worries about `docs/` dynamic import, but the actual first-order import snag is `src` packaging/PYTHONPATH.",
   "suggested_action": "Make import setup explicit before UI work: add a minimal packaging file or insert the repo `src` path at app startup, then include this in the preflight gate. Do not rely on 'run from repo root' alone.",
   "novelty_flag": true,
   "concern_type": "correctness"
  },
  {
   "id": "F-002",
   "title": "The SLC1A5 demo claim is not a clean disease-hop refutation",
   "severity": 5,
   "likelihood": 4,
   "blast_radius": 4,
   "priority": "P0",
   "evidence": "Running the real contract gives `SLC1A5 asthma Stim8hr -> answer=refuted_for_c` but hops are GATE supported, EFFECT refuted, PROGRAM supported, DISEASE refuted. `referee_triple.py:121-124` prioritizes `classification != supported` before effect/program checks, so `answer` becomes `refuted_for_c` even when EFFECT is already refuted. The demo plan says Scene 3 is a confident NO at disease hop; `docs/plans/demo_video_plan_2026-07-08.md:103-104`.",
   "concern_path": "On camera, the UI says 'refuted-for-disease' while the receipt chain shows an upstream EFFECT failure. A knowledgeable judge can read that as cherry-picking the most narratively convenient failing hop, undercutting the 'receipt-backed' thesis.",
   "suggested_action": "Either choose a refuted example whose upstream hops are genuinely green, or narrate SLC1A5 as a chain refutation with both effect and disease receipts. Round 2 must resolve whether the flagship NO is disease-specific or whole-chain.",
   "novelty_flag": true,
   "concern_type": "correctness"
  },
  {
   "id": "F-003",
   "title": "Live Claude Science trigger is substantially less Streamlit-ready than the plan implies",
   "severity": 4,
   "likelihood": 5,
   "blast_radius": 4,
   "priority": "P1",
   "evidence": "The skill requires WSL daemon lifecycle and nonce minting (`SKILL.md:13-21`, `30-40`), manual artifact discovery/copy via WSL paths (`41-48`), full-auto approval of network/data grants (`58-64`), and warns scraped text is unreliable (`66-70`). The driver hardcodes Playwright from `C:/Users/wijesingheds/.claude-c/skills/demo-video/node_modules/playwright` (`cs-drive.js:4`) and requires `--nonce` on expired auth (`cs-drive.js:29-34`).",
   "concern_path": "A Streamlit button subprocesses `node cs-drive.js`, auth has expired, `NEED_NONCE` appears, or artifacts are produced in WSL but not copied into the repo. The UI can show 'reasoning' without reliable artifacts, or hang for minutes with ambiguous status. This is exactly the fragile trap the novel claim is trying to avoid.",
   "suggested_action": "Preserve the async/precomputed split, but make live CS a disabled or operator-only stretch until there is a wrapper that handles daemon status, nonce minting, prompt file creation, artifact copy, timeout, and error display. Do not implement the raw driver call in the demo-critical app path.",
   "novelty_flag": true,
   "concern_type": "novelty"
  },
  {
   "id": "F-004",
   "title": "Three-tier workbench scope exceeds what the actual demo plan needs",
   "severity": 4,
   "likelihood": 4,
   "blast_radius": 4,
   "priority": "P1",
   "evidence": "The demo plan\u2019s settled arc is Streamlit referee hero, one funnel beat, and the pre-rendered CS figure, explicitly 'never a live S3 run' for Scene 5 (`demo_video_plan_2026-07-08.md:109-113`). The app plan makes Tier 2 and Tier 3-precomputed demo-critical and specifies a full three-screen workbench with nav, table, figure, narrative panel, themes, and optional live trigger (`streamlit_workbench_plan_2026-07-08.md:38-49`, `101-120`).",
   "concern_path": "Solo builder burns the remaining runway on a polished multi-screen app while the highest-weighted deliverable is a 180-second screen recording that only needs a reliable hero, a funnel visualization, and a CS figure/narrative beat. The app can become a product tour instead of a falsification-first research demo.",
   "suggested_action": "Keep Tier 1 as the implementation spine. Build the funnel and CS artifact as compact supporting sections only after the three flagship triples pass screen-only smoke. Treat sortable explorer, themes, and live trigger as post-preflight work.",
   "novelty_flag": true,
   "concern_type": "novelty"
  },
  {
   "id": "F-005",
   "title": "The UI contract invents downstream cards for HOP-0 failures while also claiming every value comes from `referee_triple`",
   "severity": 3,
   "likelihood": 5,
   "blast_radius": 3,
   "priority": "P1",
   "evidence": "`referee_triple.py:103-106` returns only HOP-0 when the gate fails. The plan correctly states gate-fail is only HOP-0 (`streamlit_workbench_plan_2026-07-08.md:58-60`) but the design asks EFFECT/PROGRAM/DISEASE to render greyed and struck through (`91-94`) while the risk section says every verdict/receipt comes from `referee_triple` with no hardcoding (`154-155`).",
   "concern_path": "Implementation either shows only one card, weakening the planned hero visual, or fabricates three placeholder cards and risks looking like it invented receipts. This is not fatal, but it needs an explicit UI convention: placeholders are interpretive UI state, not data hops.",
   "suggested_action": "Define synthetic downstream placeholders as presentation-only with no receipts, derived solely from `answer == 'untested'` and the HOP-0 receipt. Label them 'not evaluated' rather than treating them as returned hops.",
   "novelty_flag": true,
   "concern_type": "correctness"
  },
  {
   "id": "F-006",
   "title": "LBD cached data dependency is gitignored, so the recommended check-in needs a concrete hygiene mechanism",
   "severity": 3,
   "likelihood": 4,
   "blast_radius": 3,
   "priority": "P2",
   "evidence": ".gitignore:38-39 ignores all `data/lbd_out/`. The two needed files exist locally and are small: `sweep_Stim8hr.json` is 35,112 bytes and `lbd_questions_Stim8hr.json` is 13,241 bytes. The plan says check in a small copy is recommended (`streamlit_workbench_plan_2026-07-08.md:145-146`) but does not decide whether to force-add ignored generated outputs or move curated fixtures elsewhere.",
   "concern_path": "A clean clone or deploy lacks the cached LBD files. If they are force-added under an ignored directory, future refreshes are easy to miss and reviewers may wonder why generated data is committed despite the ignore rule.",
   "suggested_action": "Create a deliberate checked-in fixture path such as `docs/demo_fixtures/` or `app/fixtures/` containing only the two curated JSONs plus provenance. Keep `data/lbd_out/` ignored as regenerable scratch.",
   "novelty_flag": false,
   "concern_type": "correctness"
  },
  {
   "id": "F-007",
   "title": "The Claude Science artifact vocabulary does not match the app verdict taxonomy cleanly",
   "severity": 3,
   "likelihood": 4,
   "blast_radius": 2,
   "priority": "P2",
   "evidence": "`claude_science_verdict.json` uses statuses like `passed`, `supported (1 of 2 contrasts)`, `UNTESTED`, `REFUTED (at disease hop)`, and `cis-artifact WEAKENED` (`claude_science_verdict.json:8`, `20`, `47`, `52`). The app taxonomy is `supported`, `supported_weak`, `supported_flagged`, `untested`, `untested_for_c`, `refuted_for_c`, `refuted_effect`, `refuted_program` (`streamlit_workbench_plan_2026-07-08.md:97-99`).",
   "concern_path": "The CS panel cannot reuse the receipt-chain component without normalization. If rendered raw, it looks like a different referee with different semantics; if normalized silently, it may overstate CS agreement.",
   "suggested_action": "Render the CS lane as provenance/narrative evidence, not as the same deterministic verdict object, unless a small adapter maps statuses explicitly and preserves the original wording.",
   "novelty_flag": false,
   "concern_type": "correctness"
  },
  {
   "id": "F-008",
   "title": "The 'refused ~22,000' funnel wording is directionally good but numerically sloppy",
   "severity": 2,
   "likelihood": 4,
   "blast_radius": 2,
   "priority": "P2",
   "evidence": "`sweep_Stim8hr.json` funnel has `eligible_pairs: 22039`, `refuted_for_c: 21995`, `supported: 30`, `supported_weak: 10`, `supported_flagged: 3`, and `refuted_effect: 1`. The plan says 22,039 -> 30 and 'refused ~22,000' (`streamlit_workbench_plan_2026-07-08.md:107-110`).",
   "concern_path": "The funnel beat is persuasive, but if the UI implies exactly 22,009 refused or collapses weak/flagged supported cases without naming them, a reviewer can accuse it of massaging the counts. The data is strong enough that it does not need imprecise arithmetic.",
   "suggested_action": "Show the full chain class breakdown: 21,995 refuted-for-C, 1 refuted-effect, 10 weak, 3 flagged, 30 clean supported. Narrate 'about twenty-two thousand did not become clean full-chain findings' rather than 'the referee refuted the rest.'",
   "novelty_flag": false,
   "concern_type": "correctness"
  }
 ],
 "escalated_findings": [],
 "dropped_findings": [],
 "new_findings": [
  "F-001",
  "F-002",
  "F-003",
  "F-004",
  "F-005",
  "F-006",
  "F-007",
  "F-008"
 ],
 "convergence_status": "round 1 \u2014 no prior position to converge with",
 "convergence_sanding_warning": null,
 "recommended_next_question": "Round 2 must first resolve the flagship demo spine: what exact three live triples will be shown, especially the 'confident NO', and what is the minimum app surface that passes screen-only preflight before any Tier-2/Tier-3 expansion or live-CS stretch work?"
}