# Round 1 — Claude's opening position

## What this debate is settling (framing question)
You are BOTH a hostile senior reviewer AND a hackathon judge who has just **watched** the attached 12-frame
storyboard of a 2:52 demo video (see `video-context.md` for the transcript, captions, and audio metadata).
The submission is **PyZoBot Arbiter** (Anthropic × Gladstone "Built with Claude: Life Sciences", Researcher
track). A six-agent Claude judging panel already reviewed it and produced a prioritized fix register — that
register is the **artifact under debate** (`judging-panel_cs-native-video_2026-07-10.md`, appended below).

**Settle three things, grounded in what you actually SEE in the frames:**
1. **Is the panel's fix register correct, complete, and correctly prioritized** to maximize this submission's
   judging outcome (Demo 30% · Claude Use 25% · Impact 25% · Depth 20%; async round selects top 6 from
   video+repo+summary; live final round reacts to the top-6 videos)?
2. **Does the actual rendered video surface anything the text-only panel missed, over-weighted, or
   under-weighted** — visual clutter, an unreadable frame, a caption that contradicts the narration, a beat
   that drags or is confusing, a legibility problem, an on-screen honesty seam, a wasted second? The panel
   could not see the frames; you can. This is your highest-value contribution.
3. **Are any of the panel's proposed fixes wrong or harmful** — would they damage the spine, over-correct, add
   clutter, or trade a real strength for a marginal gain?

## Claude's position (the thing you are critiquing)
Claude's position is: **the panel register is substantially right and should be executed**, with Tier 0
(repo-integrity own-goals: stale README, the "validated" contradiction in shipped code, stale Th1/Th2 label,
personal-path scrub, data-license wording, CC-BY attribution at publish) done before the repo goes public, and
the quick Tier-1 wording/caption wins (R01 hook, beat-6 caption protection, hold the cached-sweep caption
through the funnel, summary rewrite, name Claude Science as Anthropic's workbench + give the reviewer-catch its
own beat, say the disease hop is an inherited nomination out loud) folded into one video re-cut. The bigger
craft moves (cold-open on the refusal; de-number the NAB2 overlay) are optional upgrades.

## PRESERVE-INTENT (do not sand these off)
The video's novel spine is deliberate and must survive your critique:
- **The confident, receipt-backed NO** (failed knockdown → *untested*, not false-negative) is the moat — the
  hero, not a footnote.
- **"The library and the lab, on one bench"** — LBD generation built INSIDE Claude Science, beside the test.
- **Calibrated honesty** — NAB2 is a *nomination*, never a discovery; live-vs-cached is scoped on-screen; the
  reviewer-catches-its-own-overclaim moment is real.
Do not push toward a flashier but less honest cut. If you think a "preserve" item is actually wrong, say so
explicitly as an open question rather than quietly sanding it.

## What I want from you (Round 1)
Return structured findings per the schema. For EACH finding: tie it to a specific frame # or transcript/caption
line where possible; rate severity/likelihood/priority; and give a concrete, minimal fix. Separate:
- **Video-visible findings** (things only the frames reveal) — prioritize these.
- **Register critique** (where the panel's prioritization or completeness is off).
- **Harmful-fix warnings** (panel fixes you'd veto or soften).
Be a real judge: if this would/wouldn't make your top 6, say why in `position_summary`. Verifying claims
against the repo files is welcomed (read-only). The panel doc + this file + `video-context.md` follow.
