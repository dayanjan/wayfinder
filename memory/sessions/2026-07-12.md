# Session: 2026-07-12 (afternoon) — Mode: full-close

Contribution/novelty audit → claims↔experiments reconciliation → traceability spine → hackathon fact sheet
→ G1 held-out-eval de-risk + codex-debate + CS health-check. (A separate earlier 2026-07-12 session did the
manuscript polish; this is a distinct thread, no session-log collision.)

## What was accomplished
1. **13-agent contribution/novelty audit** (`docs/reviews/contribution-novelty-audit_2026-07-12/`, `VERDICT.md`).
   3 extractors (2 Claude + 1 Codex) → 24 claims; 6 Claude literature-verifiers (live `search_all`); 2 adversaries
   (Claude + Codex); 2 Phase-D cross-checkers. **Verdict: do-not-publish as-is** — method novel-but-NARROW **and
   UNEVALUATED** (the FATAL); biology's eczema flank is a likely 12q13/STAT6 locus nomination. Uncited evaluated
   near-neighbors found: **Popper (arXiv 2502.09858), VERITAS (2604.12144)**.
2. **Operator caught a real miss:** the lit-audit never opened the CS confounder experiments. →
   **Claims↔experiments reconciliation** (`docs/claims-vs-experiments_2026-07-12/`, `RECONCILIATION.md` + 6
   primary-artifact recon passes). Two-directional result: experiments **RESCUE** the biology (STAT6
   "mistaken-identity" RETRACTED — 2 of 3 confounder channels experimentally CLOSED: B3 expression-cis, B4
   cluster; only B5 12q13-disease-label open), **CONFIRM** the method-never-evaluated FATAL, and surface **9
   honesty fixes** (e.g. "byte-for-byte" is literally false; 395-vs-406; a self-caught wrong-cluster bug).
3. **Anti-amnesia traceability system** (the circular-forgetting fix): canonical living
   **`docs/CLAIMS_EVIDENCE_LEDGER.md`** (claim → status → evidence artifact → source S1–S11 → critique);
   protocol wired into CLAUDE.md, NEXT_SESSION.md, MEMORY.md, lessons-learned, + auto-memory.
4. **`docs/HACKATHON.md`** — single canonical fact sheet (event/track/deadline/prizes/rules/judging/submission),
   referenced from CLAUDE.md; UNVERIFIED items flagged.
5. **G1 held-out-eval fully de-risked + spec-frozen:** as-of-T Europe PMC windowing verified; positive class
   ~1,469/22,039 (CI floor 807) → **GO Option A**; finalized `docs/plans/heldout-eval-implementation-plan_2026-07-12.md`
   and hardened it via a **3-round repo-read codex-debate** (`--preserve-intent`) → **CONVERGED, repo-verified,
   no sanding** (13 findings → 0; caught rank-within-frame, the 8-class Wayfinder order incl. `refuted_program`,
   a disease-hop-only baseline, two co-primary contrasts). **CS-driver health-check PASSED** end-to-end
   (fresh project, auto-approve, Python run, artifact, sha256 hash-verified; port fix: CS on 8000, driver defaults 8765).

## Decisions made
- **Do not publish the manuscript as an evaluated discovery paper** without closing G1 (the evaluation). Biology
  is real+novel (NAB2→Th2) but the eczema disease-label is an honestly-open 12q13 confounder, not a discovery.
- **Prep/feasibility → run anywhere; anything that becomes a manuscript result → run in Claude Science** (§4.5 claim).
- G1 = **Option A time-sliced held-out eval**, frozen to plan v3 (debate-hardened).

## New gotchas discovered
- **CS runs on port 8000, but the `drive-claude-science` driver + skill default to 8765** — override
  `--url http://localhost:8000/` and mint the nonce on `:8000`. (This was the 2026-07-11 "fragile" cause.)
- Codex `exec --output-schema` logs contain MANY draft JSON objects (reasoning trace); extract the **last**
  round-N object, not the largest (the largest may be an echoed prior-round JSON from the prompt).

## New patterns noted
- **Reconcile claims against BOTH literature AND the in-repo experiment corpus** — a lit-only audit forgets what
  was already tested (the NAB2/STAT6 miss). The `CLAIMS_EVIDENCE_LEDGER.md` + read-first protocol enforce it.
- **Repo-read codex-debate on an implementation plan = specification-completion** (§22): Codex verifies claims
  against the code (caught the omitted `refuted_program` verdict class); findings arrive checked → converge fast.

## Open questions for next session
- Will the as-of-T `ab`-gate re-sizing keep the positive class ≥100 (plan v3 build-time checkpoint)?
- Does a NAB2 CD4⁺ cis-eQTL exist (gates whether G2 12q13 colocalization is resolvable, or B5 stays open)?

## Commits: `e626b4c` → `4952c9a` (all pushed). Tree clean (only transient codex .log traces, now gitignored).

---
## Handoff mirror (= memory/NEXT_SESSION.md)
## Next session priorities — written 2026-07-12 (full-close)

**Current state**: MANUSCRIPT-strengthening thread. The **contribution audit + claims↔experiments reconciliation
+ traceability spine** are DONE. **G1 (the held-out eval = the publish gate) is FULLY DE-RISKED and SPEC-FROZEN
and READY TO BUILD:** sizing GO Option A (~1,469 positives), plan **v3** codex-debate-hardened (converged,
repo-verified, no sanding), and **CS-driver health-check PASSED** (CS drivable on port 8000). Commits
`e626b4c`→`4952c9a`, all pushed; tree clean.

**Next action**: **BUILD the G1 held-out eval to plan v3** (`docs/plans/heldout-eval-implementation-plan_2026-07-12.md`).
Recommended shape: write the codex-rescue brief FROM v3 and launch it **[CODEX-RESCUE]** (a debate-hardened spec
should land near one-shot), then **[CLAUDE]** run it in CS + integrate. Concrete build order (from v3):
1. Add `cooccur_count_asof(a,b,T)` to `src/arbiter/lbd/sources.py` (append `FIRST_PDATE:[... TO {T}]`; VERIFIED).
2. Enumerate the novel-at-T frame exactly (`ac_lit_asof(2016)≤1`) + a committed **count manifest** (freeze/hash
   query,result,date) + the **blinded feasibility gate** (label counts only; re-confirm positives ≥100 after the
   as-of-T ab-gate — the plan's build-time checkpoint).
3. Implement 6 rankers — Wayfinder (exhaustive 8-class verdict order incl. `refuted_program`, then §3.2 score
   as-of-T, `ac_known`=0), **B-disease-hop-only** (min-FDR collapse mirroring `_hop3_for_disease`), B-lit-rarity,
   B-effect, B-enrichment-continuous, B-random.
4. Two **co-primary** contrasts (C-broad vs lit-rarity; C-mech vs disease-hop-only) + gene&disease two-way-clustered
   paired-difference bootstrap CIs; **report a null straight** per the frozen joint-outcome table.
5. **Run in Claude Science** (native; CS drivable on **port 8000**, driver defaults 8765 — override
   `--url http://localhost:8000/`, mint nonce on :8000) → §4 paragraph + one figure. Fallback: local + say-so.

Then (lower priority): G3 (frozen EGR receipt), the 9 manuscript honesty fixes, G2 eQTL-existence spike.

**Prerequisites**: none blocking. `search_all` + as-of-T windowing verified. CS up (pid on :8000). latexmk broken
(4-pass `pdflatex; bibtex main; pdflatex; pdflatex`).

**Open questions**: does the as-of-T ab-gate keep positives ≥100 (build checkpoint)? does a NAB2 CD4⁺ cis-eQTL
exist (gates G2)?

**Do not touch**: submission artifacts / demo video (fire-ready; "scrub and flip"). Do NOT re-run `build_tex.py`.
Dated audit/reconciliation/debate records are IMMUTABLE — cite; update `docs/CLAIMS_EVIDENCE_LEDGER.md` for status.

**Context to preload** (≤10): `docs/CLAIMS_EVIDENCE_LEDGER.md` (READ FIRST); `docs/plans/heldout-eval-implementation-plan_2026-07-12.md`
(the v3 build spec); `docs/reviews/codex-debate_heldout-eval-plan_2026-07-12.md` (debate synthesis);
`docs/claims-vs-experiments_2026-07-12/RECONCILIATION.md`; `docs/claims-vs-experiments_2026-07-12/NEW_EXPERIMENTS.md`;
`docs/reviews/contribution-novelty-audit_2026-07-12/VERDICT.md`; `src/arbiter/lbd/{sources,propose,referee_triple}.py`;
`memory/NEXT_SESSION.md`.

**Estimated budget**: codex-rescue build ≈ 1 session; CS run + integration ≈ ½–1 session.

## Mirror of this handoff is appended to memory/sessions/2026-07-12.md by session-closer.

---

# Session: 2026-07-12 (autonomous G1 build) · Mode: full-close

## What was accomplished
Operator out for the day; instruction: "do as much as possible on your own, brainstorm Claude↔Codex, get
Claude Science to do the planned work, record everything auditably" + "stay on mission — a proper manuscript."
**Built, ran, and integrated G1 (the held-out evaluation = the manuscript's publish gate).** Full auditable
record: `docs/g1-build-log_2026-07-12.md` (T0–T12).
- **Recon** resolved the load-bearing frame ambiguity from the debate record: frame = full A×C = 47,220
  novel-at-2016; Wayfinder ranks the whole frame by its 8-class verdict (gate = ranking signal, not a frame filter).
- **Codex delegation blocked** (Windows 0.144.1 sandbox bug `split writable root sets`; `danger-full-access`
  classifier-denied; config fix operator-gated with operator out) → **direct-wrote** the harness per doctrine:
  `sources.cooccur_count_asof` + `arbiter.eval.{fetch,enumerate_frame,rankers,metrics,run_eval}` (~950 LOC),
  11/11 unit tests green.
- **Blinded feasibility gate** re-confirmed GO on the frozen frame. **Read-only `codex review` WORKS despite the
  write-sandbox bug** — caught the load-bearing bug (two-way cluster bootstrap resampling per-pair → spuriously
  narrow CIs) + 2 more; all fixed before any result computed.
- **Enumeration** (local, robust, cached/concurrent, 0 failures): 22,437 novel-at-2016 pairs, 5,570 positives
  (24.8% base rate). **Metrics → NULL at the pre-registered primary**: C_broad +0.20 [−0.20,+0.65], C_mech
  −0.15 [−0.35,+0.30], joint `broad_null`; robust across k=3/k=5/pure-disjoint. Wayfinder best on secondary
  p@5 (0.80) + MAP (0.287) but not primary; disease-hop-only matches/beats it at p@20 → measured face of §4.1b.
- **Integrated**: new §4.7 + Fig 5, §4 intro six→seven, §5.3b step-1 future→done-with-null, ledger central-item
  updated. Manuscript compiles clean, 0 errors, 32→34 pp. Commits `8858d67`→`a3f3141`, all pushed.

## Decisions made
- Direct-write over Codex when Codex is sandbox-blocked + operator can't unblock (doctrine §1 fallback).
- Enumeration runs LOCALLY (94k live queries infeasible via headless CS for hours; plan v3 sanctions local+say-so).
- Report the NULL straight (honest-null commitment); do NOT promote secondary metrics (p@5/MAP) to a headline.
- CS independent-verification deferred as an optional next step (limited marginal value for a null already
  backed by a committed reproducible manifest + independent codex code-review + unit tests).

## New gotchas
- codex-cli 0.144.1 on Windows: `-s workspace-write` (unelevated sandbox) fails with "cannot enforce split
  writable root sets"; `danger-full-access` is classifier-denied for autonomous agents. **Read-only `codex
  review` still works** — reclaim the cross-model check that way when the write path is broken.
- Europe PMC throttles sustained >~8 rps from one IP (bursty backoff at 14 rps); steady 10 workers/8 rps is clean.

## Open questions for next session
- Does the null hold under an external ground-truth panel and a larger disease panel (>12 clusters raise power)?
- Given G1 is now measured (null), re-assess the manuscript's publishability framing (the audit's "do not
  publish until measured" is satisfied; the contribution is now honestly "prioritization + abstention +
  falsification with an evaluated null").

---

## HANDOFF MIRROR (next session priorities — 2026-07-12 autonomous G1 build)
## Next session priorities — written 2026-07-12 (full-close, autonomous G1 build)

**Current state**: **G1 (the held-out evaluation = the manuscript's publish gate) is BUILT, RUN, and
INTEGRATED.** The audit's central FATAL — "method demonstrated, never measured" — is **CLOSED**: the method is
now *measured*, and the honest result is a **NULL at the pre-registered primary metric**, reported straight in
new **§4.7 + Fig 5**. Manuscript compiles clean (**34 pp, 0 errors**). Full audit trail:
`docs/g1-build-log_2026-07-12.md`. Commits `8858d67`→`a3f3141`, all pushed; tree clean.

**G1 result (for context)**: frame 22,437 novel-at-2016 pairs, 5,570 positives (24.8%). C_broad (Wayfinder−lit-
rarity) +0.20 [−0.20,+0.65]; C_mech (Wayfinder−disease-hop-only) −0.15 [−0.35,+0.30]; joint `broad_null`, robust
across k=3/k=5/pure-disjoint. Wayfinder leads on secondary p@5 (0.80) + MAP (0.287) but not the primary; disease-
hop-only matches/beats it at p@20 — the measured face of §4.1b's substrate-inherited stringency.

**Next action** (pick one; ordered by value):
1. **Apply the 9 manuscript honesty fixes** from `docs/claims-vs-experiments_2026-07-12/NEW_EXPERIMENTS.md`
   (M6 "byte-for-byte"→"value-identical"; M5 "flagged & removed" wording; R2 funnel 395↔406; M8/R4 rank-
   stability conditions; B6 T/NK n.s.; retire the buggy [74,90] script cite; stale "1.9 kb"; M7 wording; B7
   EGR2 caveat). Cheap, independent, honest-record hygiene. **[CLAUDE]**
2. **Re-assess the manuscript contribution framing** now that G1 is measured (null): the paper is honestly
   "receipt-backed prioritization + abstention + falsification with an evaluated null"; confirm the abstract +
   §1 + §5 lead with that and don't overclaim ranking superiority. **[CLAUDE]**
3. **(Optional) CS independent-verification of G1 metrics**: drive CS (:8000) to recompute the two co-primary
   contrasts from the committed manifest + reviewer-verify +0.20/−0.15/broad_null (a §4.5-consistent check).
   Lower priority — the null is already backed by the committed manifest + codex code-review + unit tests. **[CLAUDE]**
4. **G3 (frozen EGR receipt)** — re-run `nab2_egr_mechanism_check.py`, commit the output JSON (R5). **[CLAUDE — small]**

**Prerequisites**: none blocking. Manuscript compiles via 4-pass `pdflatex; bibtex main; pdflatex; pdflatex`
(latexmk broken). CS up on :8000 if pursuing #3.

**Open questions**: does the null survive an external ground-truth panel + a larger (>12) disease panel (raises
power)? Given G1 is now measured, is the paper publishable as-is (audit's "do not publish until measured" is met)?

**Do not touch**: submission artifacts / demo video (fire-ready; "scrub and flip"). The committed G1 receipts
(`data/eval_out/count_manifest_full_T2016_k5.json`, `eval_results_T2016_k5.json`, `sensitivity.json`) and the
dated audit/reconciliation/debate records are IMMUTABLE — cite, don't edit; update `CLAIMS_EVIDENCE_LEDGER.md`.

**Context to preload** (≤10): `docs/g1-build-log_2026-07-12.md`; `docs/CLAIMS_EVIDENCE_LEDGER.md`;
`docs/manuscript/latex/sections/04_results.tex` (§4.7); `data/eval_out/SUMMARY.md`;
`docs/claims-vs-experiments_2026-07-12/NEW_EXPERIMENTS.md`; `docs/reviews/diff_g1-harness_2026-07-12.md`;
`src/arbiter/eval/`; `memory/NEXT_SESSION.md`.

**Estimated budget**: honesty fixes ≈ ½ session; contribution reframe ≈ ½ session; CS verify ≈ ½–1 session.

## Mirror of this handoff is appended to memory/sessions/2026-07-12.md by session-closer.
