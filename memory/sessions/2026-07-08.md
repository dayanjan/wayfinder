# Session — 2026-07-08 (LBD proposer built, finding replicated + source-paper-vetted)

## What was accomplished
Built the **LBD question-proposer** end-to-end and drove one finding through generation → cull →
independent replication → external vetting.

1. **Spec hardened v1→v2** via a 3-round repo-read codex-debate (9→3→0 findings) + an independent
   Fable-5 read. Fixed: referee couldn't answer a *specific* disease; absent entity-A source tables;
   B over-scoped to 3 programs.
2. **Fresh tool layer** `src/arbiter/lbd/` (new-work-only, all verified live): entity_maps (disease
   ids resolved to **MONDO not EFO** via Open Targets/OLS), `_http`/`sources` (Europe PMC / Open
   Targets / MyGene), `entities` (answer-free A universe), `referee_triple` (exact-disease adapter),
   `cooccur` + `propose` (gated novelty score + full sweep). A Codex code consult caught + we fixed a
   scoring-rewards-obscurity issue and a full-chain verdict bug.
3. **Full Stim8hr sweep:** 3,935 genes → 22,039 candidate questions → **30 clean full-chain
   referee-supported**. Headline: **NAB2 → Th1/Th2 → atopic eczema**.
4. **Independent literature audit** (4-agent team via new `src/arbiter/lit/`): NAB2→Th1/Th2 and
   NAB2→atopic eczema BOTH novel (0 direct papers); surfaced the **STAT6-adjacency** confounder.
5. **In-data confounder checks:** STAT6-locus (`nab2_stat6_confounder_check.py`) and EGR-mediation
   (`nab2_egr_mechanism_check.py`) both argued-against.
6. **5-agent independent replication** (3 Opus + 2 Codex, 2 clean-room re-impls; `docs/replication/`):
   **UNANIMOUS PASS** — every headline number reproduced to the unit; caught+fixed a cluster-ID bug
   (74→90/100), a stat overstatement (8× is effect-size, ~3× on z), and reframed which arguments carry.
7. **Source-paper read** (`docs/source_paper_read_eczema_2026-07-08.md`): paper never mentions NAB2
   (novelty confirmed); disease labels are Open Targets GWAS-genetic (LD-susceptible, no coloc control);
   sharpest concern = CRISPRi **cis-artifact** — tested (`nab2_cis_artifact_check.py`): NAB2 & STAT6
   don't co-cluster + NAB2 reproducible (R 0.74) → argues against cis.

## Decisions made
- **Reframed the finding** from "consistent with a novel link" to a **novel, reproducible NOMINATION
  with the atopic-eczema disease link FLAGGED** (STAT6 shadow argued-against, not fully excluded) —
  matching the source authors' own nomination framing. More conservative, more honest, on-thesis.
- Source paper kept in `references/` **gitignored** (preprint — cited, not redistributed); analysis
  repo `github.com/emdann/GWT_perturbseq_analysis_2025` recorded durably.
- Full independent-validation audit trail (agent prompts + reports + methodology) version-controlled
  under `docs/replication/`.

## Key artifacts
- Finding: `docs/lbd_finding_nab2_2026-07-08.md` · Process log: `docs/lbd-build-log.md`
- Knowledge synthesis: `docs/nab2_knowledge_synthesis_2026-07-08.md`
- Replication: `docs/replication/` + `docs/replication_report_2026-07-08.md`
- Source-paper read: `docs/source_paper_read_eczema_2026-07-08.md`
- Reproducible checks: `docs/nab2_stat6_confounder_check.py`, `docs/nab2_egr_mechanism_check.py`,
  `docs/nab2_cis_artifact_check.py`

## Next session priorities
1. **M5 — demo video + README-as-paper** [CLAUDE for README synthesis; demo-video skill]. Needs
   operator direction on narrative/format. The finding + full validation are the paper's spine.
2. Optional: the **deposited-DE cis-check** (does NAB2-KD lower STAT6 mRNA?) via the authors' repo —
   the one test that would fully close the STAT6 caveat.
3. Optional: Rest/Stim48hr sweeps for appendix candidates.

---

# Session close (full-close) — 2026-07-08 (cont.): definitive STAT6 check + provenance + notebook plan

## What was accomplished (post-finding chapter)
- **Full raw provenance trail preserved** → `docs/provenance/raw-working-trail/` (52 verbatim artifacts:
  codex-debate run-logs + prompts, both Codex consults, LBD build scripts/outputs, and the entire
  5-agent replication trail incl. the agents' own from-scratch scripts). Literature-corpus abstracts
  stripped (third-party text not redistributed; retrieval metadata kept). Secret-scanned (values +
  patterns) — clean. Manifest at `docs/provenance/README.md`.
- **DEFINITIVE STAT6 cis-artifact check** (`docs/nab2_stat6_definitive_check.py`): read the authors'
  deposited genome-wide DE (`GWCD4i.DE_stats.h5ad`, 16.8 GB) **lazily via h5py+s3fs from the public
  no-creds S3 bucket — no full download**. Result: under NAB2 knockdown **STAT6 is unmoved**
  (log2FC +0.09, adj-p 0.79, not significant; NAB2 own on-target −3.08; STAT6 not among the 302 genes
  NAB2-KD significantly moves). A cis artifact would push STAT6 down → **cis/shadow confounder
  DEFINITIVELY EXCLUDED**. Also noted: the authors' own `offtarget_flag` (TSS within 10 kb w/ signif
  down-regulation) is False for NAB2 — their pipeline already found no cis down-reg of a neighbor.
- **Verdict upgraded** from "flagged nomination" to: a genuine, novel, NAB2-specific Th1/Th2 regulator
  with the sharpest confounder closed by external gold-standard data (remains a nomination re causality,
  per the paper's own framing). Finding + synthesis + references docs updated.

## Decisions made
- Submission format (operator + Claude agreed): build a **Jupyter notebook** = the executable evidence
  chain (the missing runnable analysis index), AND a **Claude Science evidence chain** = the "how Claude
  Science got us there" reasoning layer. Notebook is the single source of truth; Claude Science mirrors it.
- Do the notebook in a **fresh full-context session** (this close enables that). Rationale: everything is
  now durably documented, so a cold start loses nothing; the notebook imports the vetted modules anyway.

## New gotchas / patterns
- Lazy S3 h5ad read (h5py+s3fs, anon) to slice one cell of a 16.8 GB file — CPU-only, no download.
- `offtarget_flag` in DE_stats already encodes a 10 kb cis-neighbor down-regulation check.
- Provenance pattern: preserve raw trail but strip third-party copyrighted text (abstracts) → metadata only.

## Next session priorities — written 2026-07-08 (full-close)
**Current state**: M3+M4 done; finding replicated + confounders excluded (incl. definitive STAT6);
full provenance committed; tree clean. Next = M5 submission artifacts.
**Next action**: `pip install jupyterlab ipykernel nbformat`, then scaffold
`notebooks/pyzobot_arbiter_evidence_chain.ipynb` importing `arbiter.lbd`/`arbiter.lit`, running the
FAST decisive checks live (A universe, NAB2 receipt via referee_triple, the 4 confounder checks, the
definitive STAT6 S3 read) with narrative markdown; treat the ~22-min full sweep as cached.
**Prerequisites**: jupyter stack installed; `pip install -r requirements.txt` (pandas/requests/h5py/s3fs).
**Open questions**: which cells run live vs cached (sweep is heavy); notebook + Claude Science division of labor; then the 3-min demo video (heavily weighted).
**Do not touch**: never commit `.env`, `data/*.csv`, `data/lbd_cache/`, `data/lbd_out/`, `references/*.pdf`, `.claude/scratch/`.
**Context to preload**: `docs/lbd_finding_nab2_2026-07-08.md`, `docs/lbd-build-log.md`, `docs/replication/README.md`, `docs/nab2_stat6_definitive_check.py`, `src/arbiter/lbd/referee_triple.py`, `src/arbiter/lbd/propose.py`, `references/README.md`, `WORK_PROGRESS.md`, `MEMORY.md`.
**Estimated budget**: ~0.5 day for the notebook; then Claude Science evidence chain + demo video.
