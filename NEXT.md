# NEXT — moved

The async handoff now lives at **[`memory/NEXT_SESSION.md`](memory/NEXT_SESSION.md)** —
the canonical file read by `/session-start` and written by `/session-closer`.

This stub stays so old links resolve. Don't edit it; edit the handoff there.
