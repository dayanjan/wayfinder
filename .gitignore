# secrets
.env
.env.*
*.key
.claude/settings.local.json

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# node
node_modules/
dist/

# data (public; reproduce via data/fetch_data.sh)
data/*.csv

# OS / scratch
.DS_Store
Thumbs.db
.scratch/
.claude/scratch/

# codex debate/install-audit local material (raw ANSI logs, installer bytes)
memory/install-audit/

# local hackathon reference (voice memo, event notes) — keep out of public history
01-hackaton details/

# LBD proposer API cache (raw JSON; reproducible via re-query)
data/lbd_cache/

# LBD proposer sweep outputs (regenerable)
data/lbd_out/

# source paper PDF (bioRxiv preprint — referenced/cited, not redistributed)
references/*.pdf

# demo-video working dir (rendered mp4 + music mp3 — large, out-of-band; recipe is in docs/demo-video-pack/)
.tmp/
# NAB2 GEO mining: raw downloads + caches are provenance, not committed
docs/nab2-direction-geo_2026-07-09/downloads/
docs/nab2-direction-geo_*/__pycache__/

# Personal CV (contains contact info) — never publish
CV/
